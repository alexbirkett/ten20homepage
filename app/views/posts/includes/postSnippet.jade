div.post
  h1
    a(href=post.url)
      = post.title
  span.date #{post.date.getMonth()+1}/#{post.date.getDate()}/#{post.date.getFullYear()}
  div.post.content
    != post.preview
  //
    h4.more
       a(href=post.url).btn.btn-default tell me more
